class Product {
    constructor(
      id,
      name,
      image,
      information,
    ) {
      this.id = id;
      this.name = name;
      this.image = image;
      this.information = information;
    } }

export default Product;